@model MonamourWeb.ViewModels.BreedViewModel
@{
    ViewData["Title"] = "Добавить породу";
}
<form method="post" asp-controller="Breeds" asp-action="Create">
    <div class="border p-3">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group row">
            <h4 class="text-black-50 pl-3">Добавить породу</h4>
        </div>

        <br />

        <table class="table table-borderless text-center" style="width: 100%">
            <tr>
                <td>
                    <label asp-for="Breed.Title"></label>
                </td>
                <td>
                    <input asp-for="Breed.Title" class="form-control" />
                    <span asp-validation-for="Breed.Title" class="text-danger"></span>
                </td>
            </tr>
            <tr>
                <td>
                    <label asp-for="Breed.Animal"></label>
                </td>
                <td>
                    <select asp-for="Breed.AnimalId" class="form-control">
                        <option value="">Выберите животное</option>
                        @foreach (var item in Model.Animals)
                        {
                            <option value="@item.Id">@item.Title</option>
                        }
                    </select>
                    <span asp-validation-for="Breed.AnimalId" class="text-danger"></span>
                </td>
            </tr>
        </table>

        <br />

        <div class="form-group row">
            <div class="col-8 offset-2 row">
                <div class="col">
                    <input type="submit" class="btn btn-info w-75" value="Добавить" />
                </div>
                <div class="col">
                    <a class="btn btn-danger w-75" asp-controller="Breeds" asp-action="All">Породы</a>
                </div>
            </div>
        </div>
    </div>
</form>
